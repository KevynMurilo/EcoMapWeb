import styled from "styled-components";

// Definição do componente Container
export const Container = styled.div`
    width: 100%; /* Define a largura como 100% */
    overflow: hidden; /* Esconde qualquer conteúdo que ultrapasse o limite do container */
`;
